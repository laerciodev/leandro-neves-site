import React from "react"
import MenuContainer from './MenuContainer';
import "./styles.css"

const Menu = () => (
    <>
        <nav className="nav-menu">
            menu
        </nav>
        <MenuContainer />
    </>
);

export default Menu;